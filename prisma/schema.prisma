generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Inventory {
  inventoryId       String  @id @default(cuid())
  product           Product @relation(fields: [inventoryId], references: [id])
  availableQuantity Int
  costOfProduction  Int
  loadTime          Int
  reorderPoint      Int
  reorderQuantity   Int
  safetyStock       Int
  stockOnOrder      Int
}

enum OrderStatus {
  PENDING
  COMPLETED
}

model Order {
  orderId              String         @id @default(cuid())
  customerName         String
  customerEmail        String
  customerPhoneNum     String
  billingAddressLine1  String
  billingAddressLine2  String
  billingCity          String
  billingZip           String
  billingCountry       String
  shippingAddressLine1 String
  shippingAddressLine2 String
  shippingCity         String
  shippingZip          String
  shippingCountry      String
  status               OrderStatus
  createdAt            DateTime    @default(now())

  ProductToOrder ProductToOrder[]
}

model Product {
  id                 String              @id @default(cuid())
  inventory          Inventory?
  name               String?
  price              Int
  description        String
  details            String
  weight             Int
  height             Int
  width              Int
  depth              Int
  specialLabelNeeded Boolean
  ProductToOrder     ProductToOrder[]
  ProductoToTag      ProductoToTag[]
}

model ProductToOrder {
  productId String
  orderId   String
  quantity  Int
  product   Product @relation(fields: [productId], references: [id])
  order     Order   @relation(fields: [orderId], references: [orderId])

  @@id(name: "id", fields: [productId, orderId])
}

model ProductoToTag {
  productId String
  tagName   String
  product   Product @relation(fields: [productId], references: [id])
  tag       Tag     @relation(fields: [tagName], references: [name])

  @@id(name: "id", fields: [productId])
}

model Tag {
  name          String          @unique
  ProductoToTag ProductoToTag[]
}

model User {
  Id                   String     @id @default(cuid())
  name                 String?
  email                String  @unique
  password             String
  shippingAddressLine1 String
  shippingAddressLine2 String
  shippingCity         String
  shippingZip          String
  shippingCountry      String
}

model Authentication {
  id        String   @id @default(cuid())
  email     String   @unique
  password  String   @unique
}
