model Products {
  id                   String  @id @default(uuid())
  inventory            Inventory? @relation("product_inventory")
  name                 String
  price                Float
  description          String
  details              String
  weight               Int
  height               Int
  width                Int
  depth                Int
  special_label_needed Boolean
}